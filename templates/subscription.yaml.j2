---

apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ sub_name }}
  namespace: {{ namespace }}
spec:
  source: {{ source }}
  sourceNamespace: {{ source_namespace }}
  name: {{ package_name }}
  startingCSV: {{ starting_csv }}
  channel: {{ channel }}
